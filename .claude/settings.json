{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Write|Edit",
        "hooks": [
          {
            "type": "command",
            "shell": "powershell",
            "command": "$env:Path = [System.Environment]::GetEnvironmentVariable('Path','Machine') + ';' + [System.Environment]::GetEnvironmentVariable('Path','User'); git add -A; $s = git status --porcelain; if ($s) { git commit -m 'auto-save' | Out-Null; git push 2>$null }; exit 0",
            "timeout": 30,
            "statusMessage": "Auto-saving to GitHub..."
          }
        ]
      }
    ]
  }
}
